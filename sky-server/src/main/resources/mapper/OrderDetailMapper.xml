<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertAll">
        insert into order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, amount,number)
        VALUES
            <foreach collection="orderDetailList" separator="," item="o">
                (#{o.name}, #{o.image}, #{o.orderId}, #{o.dishId}, #{o.setmealId}, #{o.dishFlavor}, #{o.amount},#{o.number})
            </foreach>
    </insert>
</mapper>
